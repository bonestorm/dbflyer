
$(function(){
 
// Add the Slist class
slist = function(opts) {

  // the object we're encapsulating
  // show_grid: whether to show the cells and quads of the grid or not
  var OBJ = {
    show_grid: true
  };

  
  // return the object
  return OBJ;
  
};


});